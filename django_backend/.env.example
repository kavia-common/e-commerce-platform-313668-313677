# Django settings
DJANGO_SECRET_KEY=change-me
DJANGO_DEBUG=true
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

# Database (PDF mentions SQLite; default uses SQLite if DATABASE_URL is not provided)
# If you want PostgreSQL, provide DATABASE_URL like:
# DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DBNAME
DATABASE_URL=

# Site URLs (optional; used for redirects/navigation)
SITE_URL=http://localhost:8000
